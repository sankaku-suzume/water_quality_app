.container
  .samples_header
    %h1.samples-header_title 検体一覧
    .samples-header_btn
      = link_to '新規作成', new_sample_path, class: "btn btn-new"
  .samples_samples
    - @samples.each do |sample|
      .samples_sample
        = link_to plant_sample_path(sample.plant, sample) do
          .sample_date= l(sample.sampling_date, format: :long)
          .sample_plant= sample.plant.name
    = paginate @samples
  .actions
    = link_to "戻る", root_path, class: "btn btn-show"

  .div-comment
    %p 採水検体データを事業場関係無しに新しい順に表示しています。